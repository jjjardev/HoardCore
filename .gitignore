# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# --- Virtualenv ---
venv/
.venv/

# --- Testing ---
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Runtime data & vault ---
# The SQLite vault and per-domain extracted/binary content live under
# hoardcore_data/ and should NOT be committed. Re-ingest to rebuild.
hoardcore_data/

# --- Research artifacts (local deliverables only) ---
artifacts/

# The top-level peel_anchor/ directory (deterministic LCS approx + the
# streaming-edit-distance sketch research) is COMMITTED and tracked, so the
# claims are provable. It is the proof-of-record for the frontier-synthesis
# stress test described in README.md.

# Local config — auto-generated on first run and may contain cookies/credentials
hoardcore.toml

# External reference implementation (third-party MIT source used for porting)
densevault-1.py

# --- Editors / OS ---
.DS_Store
*.swp
.idea/
.vscode/